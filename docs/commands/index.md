# JiETNG 命令完整列表

本文档列出了 JiETNG LINE Bot 的所有可用命令。

---

## 目录

- [基础命令](#基础命令)
- [账户管理](#账户管理)
- [成绩查询](#成绩查询)
- [歌曲查询](#歌曲查询)
- [版本达成](#版本达成)
- [好友功能](#好友功能)
- [工具命令](#工具命令)

---

## 基础命令

### 支持功能

| 命令 | 别名 | 说明 |
|------|------|------|
| `donate` | 无 | 显示捐赠支持信息，支持 JiETNG 的开发与未来 |
| `status` | 无 | 显示机器人运行状态（运行时长、CPU、内存等） |
| `command` | `cmd` | 显示可用的成绩命令列表 |

---

## 账户管理

### 绑定与查看

| 命令 | 别名 | 说明 |
|------|------|------|
| `bind` | 无 | 绑定 SEGA 账户 |
| `profile` | `getme` | 查看当前账户绑定信息 |
| `unbind` | 无 | 解除账户绑定 |
| `settings` | `rebind` | 修改账户设置（密码、时区、语言、版本等），无需解绑重新绑定 |

### 数据更新

| 命令 | 别名 | 说明 |
|------|------|------|
| `maimai update` | `update` | 从 maimai NET 同步最新成绩数据 |

---

## 成绩查询

### 标准成绩图

| 命令 | 别名 | 说明 |
|------|------|------|
| `b50` | `best50` | Best 35 (旧版本) + Best 15 (新版本) |
| `b40` | `best40` | Best 25 (旧版本) + Best 15 (新版本) - 过去的 Rating 计算方案 |
| `b100` | `best100` | Best 70 (旧版本) + Best 30 (新版本) |
| `b35` | `best35` | 仅显示旧版本 Best 35 |
| `b15` | `best15` | 仅显示新版本 Best 15 |

### 特殊成绩图

| 命令 | 别名 | 说明 |
|------|------|------|
| `ab35` | `allb35` | 所有版本混合的 Best 35 |
| `ab50` | `allb50` | 所有版本混合的 Best 50 |
| `ab100` | `allb100` | 所有版本混合的 Best 100 |
| `apb50` | `ap50` | 仅显示 AP/AP+ 的 Best 50 |
| `fdxb50` | `fdx50` | 仅显示 FDX/FDX+ 的 Best 50 |
| `rct50` | `r50` | 最近 50 条游玩记录 |
| `idealb50` | `idlb50` | 理想分数 Best 50 |

### 高级筛选参数

所有成绩图命令都支持以下筛选参数：

| 参数 | 说明 | 示例 |
|------|------|------|
| `-lv [值] [max]` | 按定数筛选。单值 = 精确匹配；双值 = 范围筛选 | `b50 -lv 13.2 13.8` 或 `b50 -lv 13.7` |
| `-ra [值] [max]` | 按 Rating 筛选。单值 = 精确匹配；双值 = 范围筛选 | `b50 -ra 301 312` 或 `b50 -ra 301` |
| `-star [值] [max]` | 按 DX 星数筛选。单值 = 精确匹配；双值 = 范围筛选 | `b50 -star 3 5` 或 `b50 -star 3` |
| `-scr [min] [max]` | 按达成率筛选（max 可省略，默认无上限） | `b50 -scr 100.3 100.8` 或 `b50 -scr 100.3` |
| `-dx [min] [max]` | 按 DX 分数百分比筛选（max 可省略，默认无上限） | `b50 -dx 92 95` 或 `b50 -dx 92` |

**注意**：不同参数的筛选方式不同：
- `-lv 13.7` 表示定数 = 13.7（精确匹配）；使用 `-lv 13.5 14.0` 指定范围
- `-ra 301` 表示 Rating = 301（精确匹配）；使用 `-ra 301 312` 指定范围
- `-star 3` 表示 DX 星数 = 3（精确匹配）；使用 `-star 3 5` 指定范围
- `-scr 100.3` 表示达成率 ≥100.3%（无上限）
- `-dx 92` 表示 DX 分数 ≥92%（无上限）

#### 筛选参数组合示例

```
b50 -lv 13.2 13.8                    # 定数 13.2-13.8 的 B50
b50 -lv 13.7                         # 定数精确为 13.7 的 B50
b50 -ra 301 312                      # Rating 301-312 的 B50
b50 -ra 301                          # Rating 精确为 301 的 B50
b50 -star 3 5                        # DX 星数 3-5 的 B50
b50 -star 3                          # DX 星数精确为 3 的 B50
b50 -scr 100.3 100.8                 # 达成率 100.3%-100.8% 的 B50
b50 -scr 100.3                       # 达成率 ≥100.3% 的 B50（无上限）
b50 -dx 92 95                        # DX 分数 92%-95% 的 B50
b50 -dx 92                           # DX 分数 ≥92% 的 B50（无上限）
b50 -lv 13.2 13.8 -scr 100.0         # 定数 13.2-13.8 且达成率 ≥100% 的 B50
b100 -lv 13.0 14.9 -dx 92 95         # 定数 13.0-14.9 且 DX 92%-95% 的 B100
idealb50 -lv 13.5 14.0               # 定数 13.5-14.0 的理想 B50
```

## 歌曲查询

### 歌曲信息

| 命令格式 | 别名 | 说明 | 示例 |
|---------|------|------|------|
| `[曲名]ってどんな曲` | `[曲名]info`, `[曲名]song-info` | 查询歌曲详细信息 | `ヒバナってどんな曲` |
| `[曲名]のレコード` | `[曲名]record`, `[曲名]song-record` | 查看个人在该曲的成绩 | `ヒバナのレコード` |

### 等级成绩列表 & 定数列表

| 命令格式 | 别名 | 说明 | 示例 |
|---------|------|------|------|
| `[等级]のレコードリスト (页码)` | `[等级]record-list (页码)`, `[等级]records (页码)` | 查看指定等级的成绩 | `13のレコードリスト` |
| `[定数]のレコードリスト (页码)` | `[定数]record-list (页码)`, `[定数]records (页码)` | 查看指定定数的成绩 | `14.5のレコードリスト` |

**说明**：
- **等级列表**：输入整数（1-15）查看该等级下所有谱面
  - 示例：`13のレコードリスト` - 查看所有 Lv.13 谱面的成绩
- **定数列表**：输入小数查看指定定数的所有谱面
  - 示例：`14.5のレコードリスト` - 查看所有定数 14.5 的谱面成绩
  - 示例：`13.7のレコードリスト` - 查看所有定数 13.7 的谱面成绩
- 每页最多显示 50 首（旧版本最多 35 首 + 新版本最多 15 首），可通过页码参数翻页
- 自动按 Rating 降序排列

---

## 版本达成

### 牌子达成情况

**命令格式**：`[版本姓名框]の達成状況`

**别名**：可以使用 `achievement` 替代 `の達成状況`

| 牌子类型 | 命令示例 | 说明 |
|---------|---------|------|
| 極（极） | `宴極の達成状況` | 查看"宴極"牌达成情况 |
| 将（将） | `双将の達成状況` | 查看"双将"牌达成情况 |
| 神（神） | `鏡神の達成状況` | 查看"鏡神"牌达成情况 |
| 舞舞（舞舞） | `彩舞舞の達成状況` | 查看"彩舞舞"牌达成情况 |

**更多示例**：
```
宴極の達成状況                # 宴 极
双将の達成状況                # 双 将
鏡神の達成状況                # 鏡 神
彩舞舞の達成状況              # 彩 舞舞
真極の達成状況                # 真 极
真将の達成状況                # 真 将
真神の達成状況                # 真 神
真舞舞の達成状況              # 真 舞舞
```

**英文别名示例**：
```
宴極achievement-list            # 等同于 宴極の達成状況
双将achievement                 # 等同于 双将の達成状況
```

### 版本歌曲列表

| 命令格式 | 别名 | 说明 | 示例 |
|---------|------|------|------|
| `[版本名]のバージョンリスト` | `[版本名] version-list` | 查看该版本的所有歌曲 | `PRiSM PLUSのバージョンリスト` |

**版本名示例**：
```
PRiSM PLUSのバージョンリスト
FESTiVAL PLUSのバージョンリスト
BUDDiESのバージョンリスト
UNiVERSEのバージョンリスト
```

---

## 好友功能

### 好友管理

| 命令 | 别名 | 说明 |
|------|------|------|
| `friend list` | `friends` | 查看已添加的好友列表 |
| `friend-rcd [好友代码] [图表类型?] [过滤器?]` | 无 | 查看好友的成绩图表 |

**示例**：
```
friend list                          # 查看好友列表
friend-rcd 1234567890123456          # 查看好友 B50（默认）
friend-rcd 1234567890123456 b100     # 查看好友 B100
friend-rcd 1234567890123456 b50 -lv 14.7   # 查看好友 B50（定数 14.7）
```

**说明**：
- 需要先绑定 SEGA 账户才能使用好友功能
- `[图表类型]` 支持所有 B 系命令（b50、b100、ab50 等），默认为 b50
- `[过滤器]` 支持与 b50 相同的过滤参数（-lv、-ra、-scr 等）
- 仅限私聊使用，不支持群聊

---

## 工具命令

### Rating 计算

| 命令 | 说明 | 示例 |
|------|------|------|
| `rc [定数]` | 查看指定定数的 Rating 对照表 | `rc 13.2` |

### 分数计算

| 命令 | 说明 | 示例 |
|------|------|------|
| `calc [tap] [hold] [slide] (touch) [break]` | 计算 note 分数 | `calc 500 100 200 50 50` |

**说明**：
- 计算器会显示各类失误（Great/Good/Miss）对应的达成率
- 支持 4 参数（无 touch）和 5 参数（含 touch）两种格式

### 随机选曲

| 命令 | 说明 | 示例 |
|------|------|------|
| `random (等级/定数)` | 随机选择一首歌曲 | `random 14` |

### 位置服务

| 功能 | 说明 |
|------|------|
| 发送位置 | 查找附近的 maimai 游戏厅 |

**说明**：
- 在 LINE 中点击"+"按钮，选择"位置"
- 发送当前位置或任意位置
- Bot 会返回附近的 maimai 游戏厅信息

### 排行榜

| 命令 | 别名 | 说明 |
|------|------|------|
| `rank` | `ranking`, `ランキング` | 查看当前版本的用户 DX Rating 排行榜 |
| `rank jp` | `ranking jp`, `ランキング jp` | 查看日服 DX Rating 排行榜 |
| `rank intl` | `ranking intl`, `ランキング intl` | 查看国际服 DX Rating 排行榜 |

---

## @提及功能

在群聊中提及（@）另一位已注册 JiETNG 的用户，可查看对方的成绩数据。

**使用方式**：提及用户 + 命令（提及可在命令前或命令中任意位置）

**示例**：
```
@好友名 b50            # 查看好友的 B50
@好友名 profile        # 查看好友的账户信息
@好友名 14 record-list # 查看好友 Lv14 成绩列表
```

**说明**：
- 被提及的用户需已注册 JiETNG 账户，否则回退为查看自己的数据
- 支持成绩查询相关的所有命令（b50、b100、record-list、progress 等）

